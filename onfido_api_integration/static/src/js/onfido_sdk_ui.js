odoo.define('onfido_api_integration.onfido_portal', function (require) {
  'use strict';

  var core = require('web.core');
  var publicWidget = require('web.public.widget');
  var time = require('web.time');
  var session = require('web.session');

  var _t = core._t;

  publicWidget.registry.onfido = publicWidget.Widget.extend({
  });
});
